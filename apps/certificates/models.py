"""
Certificates app — PDF generation logic lives here.

The Certificate model is defined in `results.models` to avoid circular
imports. This app provides:
    - generators.py  — ReportLab PDF generation logic
    - views.py       — download endpoint
"""

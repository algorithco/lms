# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/

# Django
db.sqlite3
db.sqlite3-journal
.backups/
staticfiles/
media/

# Environment variables
.env
.env.local
.env.production
.env.prod

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Celery
celerybeat-schedule
celerybeat.pid

# Coverage
htmlcov/
.coverage
.coverage.*
coverage.xml

# mypy
.mypy_cache/

# pytest
.pytest_cache/

# Logs
*.log
.aider*

# Generated prod nginx config (built by deploy/init-letsencrypt.sh from
# nginx-http.conf / nginx-ssl.conf — never commit the live domain version)
deploy/nginx/nginx.conf

# VPS-side admin Basic Auth credential (created by setup-admin-auth.sh).
# The .example template stays tracked; the real file must never be committed.
deploy/secrets/htpasswd-admin

# EXAMPLE ONLY — never used for real auth.
# Real file: deploy/secrets/htpasswd-admin (VPS-side, git-ignored, chmod 600).
# Created by: ./deploy/setup-admin-auth.sh   (username is fixed: lmsadmin)
#
# This DISABLED entry guarantees fail-closed: even if it were mounted,
# no password can ever match the literal string "DISABLED".
lmsadmin:DISABLED
